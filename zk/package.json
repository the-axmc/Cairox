{
  "name": "zk",
  "version": "1.0.0",
  "description": "This folder contains the Groth16 circuit and workflow used for on-chain ZK verification via Garaga on Starknet. The system proves a binary threshold outcome and derives the `data_hash` as a Poseidon hash of `(market_id, value, threshold)`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.6"
  }
}
